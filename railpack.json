{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": { "commands": ["npm ci"] },
    "build":   { "commands": ["npm run migrate"] },
    "start":   { "command": "npm start" }
  }
}
